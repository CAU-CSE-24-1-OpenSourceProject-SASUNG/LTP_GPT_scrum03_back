# LTP_GPT_scrum_01

```bash
git clone {URL}

cd LTP_GPT_scrum_03

python3 -m venv venv

source venv/bin/activate

pip install -r requirements.txt

vi .env

### add .env 
OPENAI_API_KEY=sk-...
### save and exit with ":wq"

python3 main.app
```
